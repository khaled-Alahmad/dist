# ملف نموذجي لمتغيرات البيئة - نظام إدارة المدرسة
# انسخ هذا الملف إلى .env وعدّل القيم حسب بيئتك

# ==========================
# بيئة التشغيل
# ==========================
# استخدم "development" للتطوير أو "production" للإنتاج
NODE_ENV=production

# ==========================
# المنفذ (Port)
# ==========================
# المنفذ الذي سيعمل عليه التطبيق
# على Replit: استخدم 5000
# على VPS: استخدم منفذ مختلف مثل 3001
PORT=5000

# ==========================
# قاعدة البيانات (PostgreSQL)
# ==========================
# صيغة الاتصال: postgresql://username:password@host:port/database
# مثال: postgresql://school_admin:MyStr0ngP@ssw0rd@localhost:5432/school_management
DATABASE_URL=postgresql://school_admin:your_password_here@localhost:5432/school_management

# ==========================
# سر الجلسات (Session Secret) - مطلوب!
# ==========================
# ⚠️ IMPORTANT: هذا مطلوب لعمل تسجيل الدخول!
# استخدم الأمر التالي لتوليد سر عشوائي قوي (64 حرف):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=REPLACE_THIS_WITH_RANDOM_SECRET_FROM_CRYPTO

# ==========================
# المنطقة الزمنية (اختياري)
# ==========================
TZ=Asia/Riyadh

# ==========================
# ملاحظات مهمة للنشر على VPS
# ==========================
# 1. ⛔ لا تشارك هذا الملف علناً - يحتوي على معلومات حساسة
# 2. 🔑 SESSION_SECRET يجب أن يكون عشوائي وطويل (32 بايت = 64 حرف hex)
# 3. 🔒 غيّر كلمة مرور قاعدة البيانات إلى كلمة قوية
# 4. 🌐 في الإنتاج، استخدم HTTPS (NODE_ENV=production يفعّل secure cookies)
# 5. 📋 راجع VPS_DEPLOYMENT_GUIDE_AR.md للحصول على دليل النشر الكامل على VPS
# 6. 📌 على VPS: غيّر PORT إلى 3001 واستخدم Nginx كـ reverse proxy

# ==========================
# كيفية توليد SESSION_SECRET
# ==========================
# على Linux/Mac:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# على Windows PowerShell:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# النتيجة ستكون شيء مثل:
# a1b2c3d4e5f6...إلخ (64 حرف)
